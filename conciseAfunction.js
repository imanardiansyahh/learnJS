const sumNumbers = number => {
    const sum= number - number
    return sum
}

// const TotalNumbers = number => {
//    if (sum = number + number) 
//    return sum
// }

const sums = sumNumbers(4,4)
console.log(sums)